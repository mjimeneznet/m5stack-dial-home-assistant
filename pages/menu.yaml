font:
  - file: "gfonts://DM+Sans"
    id: font_menu_item
    size: 20
    bpp: 4
    glyphs: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyzÁÉÍÓÚÜÑáéíóúüñ0123456789 .-_%/"

  - file: "gfonts://DM+Sans"
    id: font_menu_center
    size: 21
    bpp: 4
    glyphs: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyzÁÉÍÓÚÜÑáéíóúüñ0123456789 .-_%/"

  - file: "gfonts://DM+Sans"
    id: font_menu_sub
    size: 12
    bpp: 4
    glyphs: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789 .-_%/:·°"

  - file: "gfonts://DM+Sans"
    id: font_light_selector_near
    size: 16
    bpp: 4
    glyphs: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyzáí0123456789 .-_%/"

color:
  - id: col_menu_bg
    hex: "000000"
  - id: col_menu_pill
    hex: "0D1537"
  - id: col_menu_accent
    hex: "576BFE"
  - id: col_menu_val
    hex: "4D5BD3"
  - id: col_menu_dim
    hex: "232A38"
  - id: col_menu_sdot_dim
    hex: "2A3348"

image:
  - file: images/bulb.png
    id: menu_img_light
    type: RGB565
    transparency: alpha_channel
    resize: 34x34
  - file: images/ac_logo.png
    id: menu_img_ac
    type: RGB565
    transparency: alpha_channel
    resize: 32x32
  - file: images/music.png
    id: menu_img_music
    type: RGB565
    transparency: alpha_channel
    resize: 34x34
  - file: images/timer.png
    id: menu_img_timer
    type: RGB565
    transparency: alpha_channel
    resize: 32x32
  - file: images/home.png
    id: menu_img_home
    type: RGB565
    transparency: alpha_channel
    resize: 34x34
globals:
  - id: menu_sel
    type: int
    initial_value: '0'
  - id: menu_func
    type: int
    initial_value: '4'
  - id: in_menu
    type: bool
    initial_value: 'false'
  - id: menu_animating
    type: bool
    initial_value: 'false'
  - id: button_debounce
    type: bool
    initial_value: 'false'

dial_carousel:
  - id: menu_carousel
    slots:
      - container: slot_far_up
        icon: slot_far_up_img
        title: slot_far_up_lbl
      - container: slot_near_up
        icon: slot_near_up_img
        title: slot_near_up_lbl
      - container: slot_center
        icon: slot_center_img
        title: slot_center_lbl
      - container: slot_near_dn
        icon: slot_near_dn_img
        title: slot_near_dn_lbl
      - container: slot_far_dn
        icon: slot_far_dn_img
        title: slot_far_dn_lbl
    center_bar: slot_center_bar
    center_sub: slot_center_sub

script:
  - id: menu_sync_selection
    then:
      - lambda: |-
          int items[5];
          int n = 0;
          if (strcmp("${timer_entity}", "timer.your_timer") != 0)
            items[n++] = 0;
          if (id(dial_lights_id).light_count() > 0)
            items[n++] = 1;
          if (strcmp("${climate_entity}", "climate.your_ac") != 0)
            items[n++] = 2;
          if (strcmp("${music_player_entity}", "media_player.your_player") != 0)
            items[n++] = 3;
          items[n++] = 4;

          const int home_idx = n - 1;
          bool found = false;
          for (int i = 0; i < n; i++) {
            if (items[i] == id(menu_func)) {
              id(menu_sel) = i;
              found = true;
              break;
            }
          }
          if (!found) {
            id(menu_func) = 4;
            id(menu_sel) = home_idx;
          }

  - id: open_menu
    then:
      - script.execute: menu_sync_selection
      - globals.set: {id: in_menu, value: 'true'}
      - script.execute: refresh_menu
      - lvgl.page.show:
          id: page_menu
          animation: MOVE_RIGHT

  - id: open_menu_back
    then:
      - script.execute: menu_sync_selection
      - globals.set: {id: in_menu, value: 'true'}
      - script.execute: refresh_menu
      - lvgl.page.show:
          id: page_menu
          animation: MOVE_RIGHT

  - id: close_menu
    then:
      - globals.set: {id: in_menu, value: 'false'}
      - lvgl.page.show:
          id: page_clock
          animation: MOVE_LEFT

  - id: menu_scroll_up
    then:
      - if:
          condition:
            lambda: 'return !id(menu_animating);'
          then:
            - globals.set: {id: menu_animating, value: 'true'}
            - lambda: |-
                lv_obj_add_flag(id(menu_arrow), LV_OBJ_FLAG_HIDDEN);

                auto on_ready = +[](void *ctx) {
                  int local_items[5];
                  int local_n = 0;
                  if (strcmp("${timer_entity}", "timer.your_timer") != 0)
                    local_items[local_n++] = 0;
                  if (id(dial_lights_id).light_count() > 0)
                    local_items[local_n++] = 1;
                  if (strcmp("${climate_entity}", "climate.your_ac") != 0)
                    local_items[local_n++] = 2;
                  if (strcmp("${music_player_entity}", "media_player.your_player") != 0)
                    local_items[local_n++] = 3;
                  local_items[local_n++] = 4;

                  id(menu_sel) = ((id(menu_sel) - 1) % local_n + local_n) % local_n;
                  id(menu_func) = local_items[id(menu_sel)];
                  id(refresh_menu).execute();
                  id(menu_animating) = false;
                };
                if (!id(menu_carousel).start_animation(
                        esphome::dial_carousel::CarouselDirection::UP, 54, on_ready, nullptr)) {
                  id(menu_animating) = false;
                }

  - id: menu_scroll_down
    then:
      - if:
          condition:
            lambda: 'return !id(menu_animating);'
          then:
            - globals.set: {id: menu_animating, value: 'true'}
            - lambda: |-
                lv_obj_add_flag(id(menu_arrow), LV_OBJ_FLAG_HIDDEN);

                auto on_ready = +[](void *ctx) {
                  int local_items[5];
                  int local_n = 0;
                  if (strcmp("${timer_entity}", "timer.your_timer") != 0)
                    local_items[local_n++] = 0;
                  if (id(dial_lights_id).light_count() > 0)
                    local_items[local_n++] = 1;
                  if (strcmp("${climate_entity}", "climate.your_ac") != 0)
                    local_items[local_n++] = 2;
                  if (strcmp("${music_player_entity}", "media_player.your_player") != 0)
                    local_items[local_n++] = 3;
                  local_items[local_n++] = 4;

                  id(menu_sel) = (id(menu_sel) + 1) % local_n;
                  id(menu_func) = local_items[id(menu_sel)];
                  id(refresh_menu).execute();
                  id(menu_animating) = false;
                };
                if (!id(menu_carousel).start_animation(
                        esphome::dial_carousel::CarouselDirection::DOWN, 64, on_ready, nullptr)) {
                  id(menu_animating) = false;
                }

  - id: menu_confirm
    then:
      - lambda: |-
          ESP_LOGI("menu", "selected func %d visual %d", id(menu_func), id(menu_sel));
          switch (id(menu_func)) {
            case 0:
              id(open_timer).execute();
              break;
            case 1:
              if (id(dial_lights_id).light_count() == 1) {
                id(dial_lights_id).select_light(0);
                id(open_selected_light).execute();
              } else if (id(dial_lights_id).light_count() > 1) {
                id(open_light_selector).execute();
              } else {
                ESP_LOGW("menu", "Lights selected but dial_lights is empty");
              }
              break;
            case 2:
              id(open_air).execute();
              break;
            case 3:
              id(open_music).execute();
              break;
            case 4:
            default:
              id(close_menu).execute();
              break;
          }

  - id: menu_on_ha_update
    then:
      - if:
          condition:
            lambda: 'return id(in_menu);'
          then:
            - script.execute: refresh_menu_subtitle

  - id: refresh_menu_subtitle
    then:
      - lambda: |-
          static char subbuf[64];
          const uint32_t ACCENT_COLORS[5] = {
            0xFB923C, 0xFDC003, 0x5ABBFD, 0xA792E8, 0x576BFE
          };

          auto format_time_short = [](int seconds, char *buf, size_t len) {
            if (seconds < 0) seconds = 0;
            const int h = seconds / 3600;
            const int m = (seconds % 3600) / 60;
            const int s = seconds % 60;
            if (h > 0)
              snprintf(buf, len, "%d:%02d:%02d", h, m, s);
            else
              snprintf(buf, len, "%d:%02d", m, s);
          };

          auto is_invalid_state = [](const std::string &value) -> bool {
            if (value.empty()) return true;
            std::string lower = value;
            for (char &c : lower) c = (char) std::tolower((unsigned char) c);
            return lower == "unknown" || lower == "unavailable" || lower == "none" || lower == "null";
          };

          const int func = id(menu_func);
          switch (func) {
            case 0: {
              const int IDLE = 0;
              const int ACTIVE = 1;
              const int PAUSED = 2;
              const int FINISHED = 3;
              const int UNAVAILABLE = 4;
              const int state = id(timer_ui_state);
              if (state == UNAVAILABLE || !id(ha_connected)) {
                snprintf(subbuf, sizeof(subbuf), "Unavailable");
              } else if (state == IDLE) {
                snprintf(subbuf, sizeof(subbuf), "No timer");
              } else if (state == FINISHED) {
                snprintf(subbuf, sizeof(subbuf), "Finished");
              } else {
                char timebuf[16];
                format_time_short(id(timer_remaining_seconds), timebuf, sizeof(timebuf));
                if (state == ACTIVE)
                  snprintf(subbuf, sizeof(subbuf), "%s left", timebuf);
                else
                  snprintf(subbuf, sizeof(subbuf), "Paused · %s", timebuf);
              }
              break;
            }
            case 1: {
              const size_t count = id(dial_lights_id).light_count();
              if (count == 0) {
                snprintf(subbuf, sizeof(subbuf), "Unavailable");
                break;
              }
              if (count == 1) {
                const auto snap = id(dial_lights_id).card_snapshot_at(0);
                if (!snap.state_valid) {
                  snprintf(subbuf, sizeof(subbuf), "Unavailable");
                } else if (!snap.is_on) {
                  snprintf(subbuf, sizeof(subbuf), "Off");
                } else if (snap.brightness_valid) {
                  snprintf(subbuf, sizeof(subbuf), "On · %d%%", snap.brightness_percent);
                } else {
                  snprintf(subbuf, sizeof(subbuf), "On");
                }
                break;
              }
              size_t on_count = 0;
              bool any_valid = false;
              for (size_t i = 0; i < count; i++) {
                const auto snap = id(dial_lights_id).card_snapshot_at(i);
                if (snap.state_valid) {
                  any_valid = true;
                  if (snap.is_on) on_count++;
                }
              }
              if (!any_valid) {
                snprintf(subbuf, sizeof(subbuf), "Unavailable");
              } else if (on_count == 0) {
                snprintf(subbuf, sizeof(subbuf), "All off");
              } else if (on_count == count) {
                snprintf(subbuf, sizeof(subbuf), "All on");
              } else {
                snprintf(subbuf, sizeof(subbuf), "%d of %d on", (int) on_count, (int) count);
              }
              break;
            }
            case 2: {
              auto format_setpoint = [](float t, char *buf, size_t len) -> bool {
                if (isnan(t)) return false;
                const float rounded = roundf(t);
                if (fabsf(t - rounded) > 0.05f)
                  snprintf(buf, len, "%.1f °C", t);
                else
                  snprintf(buf, len, "%.0f °C", t);
                return true;
              };

              auto hvac_label = [](const std::string &mode, char *buf, size_t len) -> bool {
                if (mode == "cool") snprintf(buf, len, "Cooling");
                else if (mode == "heat") snprintf(buf, len, "Heating");
                else if (mode == "fan_only") snprintf(buf, len, "Fan");
                else if (mode == "dry") snprintf(buf, len, "Dry");
                else if (mode == "auto" || mode == "heat_cool") snprintf(buf, len, "Auto");
                else if (mode == "off") snprintf(buf, len, "Off");
                else return false;
                return true;
              };

              char statebuf[16] = "";
              bool has_state = false;
              if (id(ha_air_hvac_mode).has_state() && !is_invalid_state(id(ha_air_hvac_mode).state)) {
                has_state = hvac_label(id(ha_air_hvac_mode).state, statebuf, sizeof(statebuf));
              }

              float setpoint = NAN;
              if (id(ha_air_target_temp).has_state()) {
                const float target = id(ha_air_target_temp).state;
                if (!isnan(target)) setpoint = target;
              }
              if (isnan(setpoint) && id(ha_connected)) {
                const int t2 = id(air_temp);
                setpoint = t2 / 2.0f;
              }
              if (isnan(setpoint) && id(ha_air_current_temp).has_state()) {
                const float ct = id(ha_air_current_temp).state;
                if (!isnan(ct)) setpoint = ct;
              }

              char tempbuf[16] = "";
              const bool has_temp = format_setpoint(setpoint, tempbuf, sizeof(tempbuf));

              if (has_state && has_temp)
                snprintf(subbuf, sizeof(subbuf), "%s · %s", statebuf, tempbuf);
              else if (has_state)
                snprintf(subbuf, sizeof(subbuf), "%s", statebuf);
              else if (has_temp)
                snprintf(subbuf, sizeof(subbuf), "%s", tempbuf);
              else
                snprintf(subbuf, sizeof(subbuf), "Unavailable");
              break;
            }
            case 3: {
              if (!id(media_player_status).has_state() || is_invalid_state(id(media_player_status).state)) {
                snprintf(subbuf, sizeof(subbuf), "Unavailable");
                break;
              }
              const std::string &status = id(media_player_status).state;
              if (status == "off") {
                snprintf(subbuf, sizeof(subbuf), "Off");
              } else if (status == "idle") {
                snprintf(subbuf, sizeof(subbuf), "No playback");
              } else if (status == "paused") {
                snprintf(subbuf, sizeof(subbuf), "Paused");
              } else if (status == "playing" || status == "buffering") {
                std::string title = id(ha_media_title).state;
                if (title.empty()) title = id(track_title).state;
                if (!title.empty())
                  snprintf(subbuf, sizeof(subbuf), "%s", title.c_str());
                else
                  snprintf(subbuf, sizeof(subbuf), "Playing");
              } else {
                snprintf(subbuf, sizeof(subbuf), "Unavailable");
              }
              break;
            }
            case 4:
            default:
              snprintf(subbuf, sizeof(subbuf), "Clock");
              break;
          }

          lv_label_set_text(id(slot_center_sub), subbuf);
          lv_obj_set_style_opa(id(slot_center_sub), 255, LV_PART_MAIN);
          lv_obj_set_style_text_color(id(slot_center_sub), lv_color_hex(ACCENT_COLORS[func]), LV_PART_MAIN);

  - id: menu_touch_slot
    parameters:
      slot_offset: int
    then:
      - lambda: |-
          static constexpr int OFFSETS[5] = {-2, -1, 0, 1, 2};

          int items[5];
          int n = 0;
          if (strcmp("${timer_entity}", "timer.your_timer") != 0)
            items[n++] = 0;
          if (id(dial_lights_id).light_count() > 0)
            items[n++] = 1;
          if (strcmp("${climate_entity}", "climate.your_ac") != 0)
            items[n++] = 2;
          if (strcmp("${music_player_entity}", "media_player.your_player") != 0)
            items[n++] = 3;
          items[n++] = 4;

          int slot = -1;
          for (int s = 0; s < 5; s++) {
            if (OFFSETS[s] == slot_offset) {
              slot = s;
              break;
            }
          }
          if (slot < 0)
            return;

          const int sel = id(menu_sel);
          const int idx = ((sel + slot_offset) % n + n) % n;

          int best_slot_for_index[5];
          for (int i = 0; i < n; i++)
            best_slot_for_index[i] = -1;
          for (int s = 0; s < 5; s++) {
            const int off = OFFSETS[s];
            const int item_idx = ((sel + off) % n + n) % n;
            const int dist = off >= 0 ? off : -off;
            if (best_slot_for_index[item_idx] == -1) {
              best_slot_for_index[item_idx] = s;
            } else {
              const int existing_off = OFFSETS[best_slot_for_index[item_idx]];
              const int existing_dist = existing_off >= 0 ? existing_off : -existing_off;
              if (dist < existing_dist || (dist == existing_dist && off == 0))
                best_slot_for_index[item_idx] = s;
            }
          }
          if (best_slot_for_index[idx] != slot)
            return;

          id(menu_sel) = idx;
          id(menu_func) = items[idx];
          id(menu_confirm).execute();

  - id: refresh_menu
    then:
      - lambda: |-
          static constexpr int SLOT_N = 5;
          static constexpr int OFFSETS[5] = {-2, -1, 0, 1, 2};

          int items[5];
          int n = 0;
          if (strcmp("${timer_entity}", "timer.your_timer") != 0)
            items[n++] = 0;
          if (id(dial_lights_id).light_count() > 0)
            items[n++] = 1;
          if (strcmp("${climate_entity}", "climate.your_ac") != 0)
            items[n++] = 2;
          if (strcmp("${music_player_entity}", "media_player.your_player") != 0)
            items[n++] = 3;
          items[n++] = 4;

          lv_obj_t* icons[5] = {
            id(slot_far_up_img),  id(slot_near_up_img),
            id(slot_center_img),
            id(slot_near_dn_img), id(slot_far_dn_img)
          };
          lv_obj_t* title_lbls[5] = {
            id(slot_far_up_lbl),  id(slot_near_up_lbl),
            id(slot_center_lbl),
            id(slot_near_dn_lbl), id(slot_far_dn_lbl)
          };
          lv_obj_t* slots[5] = {
            id(slot_far_up), id(slot_near_up), id(slot_center),
            id(slot_near_dn), id(slot_far_dn)
          };

          const char* TITLES[5] = {"Timer", "Lights", "AC", "Music", "Home"};
          const lv_img_dsc_t* IMGS[5] = {
            id(menu_img_timer)->get_lv_image_dsc(),
            id(menu_img_light)->get_lv_image_dsc(),
            id(menu_img_ac)->get_lv_image_dsc(),
            id(menu_img_music)->get_lv_image_dsc(),
            id(menu_img_home)->get_lv_image_dsc()
          };
          const uint32_t ACCENT_COLORS[5] = {
            0xFB923C,
            0xFDC003,
            0x5ABBFD,
            0xA792E8,
            0x576BFE
          };
          const uint32_t PILL_COLORS[5] = {
            0x2A1200,
            0x2A1E00,
            0x001A2A,
            0x150A2A,
            0x0D1537
          };

          const int sel = id(menu_sel);
          id(menu_func) = items[sel];

          int best_slot_for_index[5];
          for (int i = 0; i < n; i++)
            best_slot_for_index[i] = -1;
          for (int s = 0; s < SLOT_N; s++) {
            const int off = OFFSETS[s];
            const int idx = ((sel + off) % n + n) % n;
            const int dist = off >= 0 ? off : -off;
            if (best_slot_for_index[idx] == -1) {
              best_slot_for_index[idx] = s;
            } else {
              const int existing_off = OFFSETS[best_slot_for_index[idx]];
              const int existing_dist = existing_off >= 0 ? existing_off : -existing_off;
              if (dist < existing_dist || (dist == existing_dist && off == 0))
                best_slot_for_index[idx] = s;
            }
          }

          for (int slot = 0; slot < SLOT_N; slot++) {
            const int off = OFFSETS[slot];
            const int idx = ((sel + off) % n + n) % n;
            const int func = items[idx];
            const bool show = (best_slot_for_index[idx] == slot);

            if (!show) {
              lv_obj_add_flag(slots[slot], LV_OBJ_FLAG_HIDDEN);
              lv_obj_clear_flag(slots[slot], LV_OBJ_FLAG_CLICKABLE);
              continue;
            }

            lv_obj_clear_flag(slots[slot], LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(slots[slot], LV_OBJ_FLAG_CLICKABLE);

            const auto layout = id(menu_carousel).compute_static_layout(slot, 54);
            id(menu_carousel).apply_container_geometry(slot, layout);

            if (slot == 2) {
              lv_obj_set_style_bg_color(slots[slot], lv_color_hex(PILL_COLORS[func]), LV_PART_MAIN);
              lv_obj_set_style_bg_opa(slots[slot], 128, LV_PART_MAIN);
              lv_obj_set_style_border_color(slots[slot], lv_color_hex(ACCENT_COLORS[func]), LV_PART_MAIN);
              lv_obj_set_style_border_width(slots[slot], 2, LV_PART_MAIN);
              lv_obj_set_style_border_opa(slots[slot], 255, LV_PART_MAIN);
            } else {
              lv_obj_set_style_bg_color(slots[slot], lv_color_hex(0x0D1537), LV_PART_MAIN);
              lv_obj_set_style_bg_opa(slots[slot], layout.opa_bg, LV_PART_MAIN);
              lv_obj_set_style_border_width(slots[slot], 0, LV_PART_MAIN);
            }

            if (slot == 2) {
              const int bx = layout.x - 10;
              const int by = layout.y + 12;
              lv_obj_set_pos(id(slot_center_bar), bx, by);
              lv_obj_set_style_bg_color(id(slot_center_bar), lv_color_hex(ACCENT_COLORS[func]), LV_PART_MAIN);
              lv_obj_set_style_opa(id(slot_center_bar), 0, LV_PART_MAIN);
            }

            lv_label_set_text(title_lbls[slot], TITLES[func]);
            if (slot == 2) {
              lv_obj_set_style_text_color(title_lbls[slot], lv_color_hex(ACCENT_COLORS[func]), LV_PART_MAIN);
            } else {
              lv_obj_set_style_text_color(title_lbls[slot], lv_color_hex(0x232A38), LV_PART_MAIN);
            }
            lv_obj_set_style_opa(title_lbls[slot], slot == 2 ? 255 : layout.opa_text, LV_PART_MAIN);
            lv_img_set_src(icons[slot], IMGS[func]);
            lv_obj_set_style_opa(icons[slot], slot == 2 ? 255 : layout.opa_icon, LV_PART_MAIN);
          }

          id(refresh_menu_subtitle).execute();

          {
            const int func = items[sel];
            lv_obj_set_style_bg_color(id(slot_center_bar), lv_color_hex(ACCENT_COLORS[func]), LV_PART_MAIN);
            lv_obj_set_style_border_width(id(slot_center), 0, LV_PART_MAIN);
          }

          lv_obj_t* sdots[5] = {
            id(sdot_0), id(sdot_1), id(sdot_2), id(sdot_3), id(sdot_4)
          };
          for (int d = 0; d < SLOT_N; d++) {
            if (d >= n) {
              lv_obj_add_flag(sdots[d], LV_OBJ_FLAG_HIDDEN);
              continue;
            }
            lv_obj_clear_flag(sdots[d], LV_OBJ_FLAG_HIDDEN);
            const int func = items[d];
            if (d == sel) {
              lv_obj_set_style_bg_color(sdots[d], lv_color_hex(ACCENT_COLORS[func]), LV_PART_MAIN);
              lv_obj_set_style_opa(sdots[d], 255, LV_PART_MAIN);
              lv_obj_set_size(sdots[d], 8, 8);
              lv_obj_set_style_radius(sdots[d], 0, LV_PART_MAIN);
              lv_obj_set_style_transform_angle(sdots[d], 450, LV_PART_MAIN);
            } else {
              lv_obj_set_style_bg_color(sdots[d], lv_color_hex(0x2A3348), LV_PART_MAIN);
              lv_obj_set_style_opa(sdots[d], 180, LV_PART_MAIN);
              lv_obj_set_size(sdots[d], 4, 4);
              lv_obj_set_style_radius(sdots[d], 2, LV_PART_MAIN);
              lv_obj_set_style_transform_angle(sdots[d], 0, LV_PART_MAIN);
            }
          }

          lv_obj_add_flag(id(menu_arrow), LV_OBJ_FLAG_HIDDEN);

interval:
  - interval: 1s
    then:
      - if:
          condition:
            lambda: 'return id(in_menu);'
          then:
            - script.execute: refresh_menu_subtitle

lvgl:
  pages:
    - id: page_menu
      bg_color: col_menu_bg
      bg_opa: COVER
      scrollbar_mode: "OFF"
      widgets:

        - obj:
            align: CENTER
            width: 240
            height: 240
            radius: 120
            clip_corner: true
            bg_color: col_menu_bg
            border_width: 0
            scrollbar_mode: "OFF"
            pad_all: 0
            widgets:

              - obj:
                  id: menu_stack
                  align: TOP_LEFT
                  x: 0
                  y: 0
                  width: 240
                  height: 240
                  bg_opa: TRANSP
                  border_width: 0
                  scrollbar_mode: "OFF"
                  pad_all: 0
                  clickable: false
                  widgets:

                    - obj:
                        id: slot_far_up
                        align: TOP_MID
                        y: 6
                        width: 180
                        height: 40
                        bg_opa: TRANSP
                        border_width: 0
                        scrollbar_mode: "OFF"
                        pad_all: 0
                        clickable: true
                        on_click:
                          - lambda: 'id(menu_touch_slot).execute(-2);'
                        widgets:
                          - image:
                              id: slot_far_up_img
                              align: LEFT_MID
                              x: 2
                              src: menu_img_timer
                          - label:
                              id: slot_far_up_lbl
                              align: LEFT_MID
                              x: 38
                              text_font: font_menu_item
                              text_color: col_menu_dim
                              text: "Timer"
                              text_opa: 25%

                    - obj:
                        id: slot_near_up
                        align: TOP_MID
                        y: 42
                        width: 180
                        height: 40
                        bg_opa: TRANSP
                        border_width: 0
                        scrollbar_mode: "OFF"
                        pad_all: 0
                        clickable: true
                        on_click:
                          - lambda: 'id(menu_touch_slot).execute(-1);'
                        widgets:
                          - image:
                              id: slot_near_up_img
                              align: LEFT_MID
                              x: 2
                              src: menu_img_light
                          - label:
                              id: slot_near_up_lbl
                              align: LEFT_MID
                              x: 38
                              text_font: font_menu_item
                              text_color: col_menu_dim
                              text: "Lights"
                              text_opa: 50%

                    - obj:
                        id: slot_center_bar
                        align: TOP_MID
                        y: 108
                        x: -96
                        width: 6
                        height: 32
                        radius: 3
                        bg_color: col_menu_accent
                        bg_opa: TRANSP
                        border_width: 0
                        scrollbar_mode: "OFF"
                        pad_all: 0
                        clickable: false
                    - obj:
                        id: slot_center
                        align: TOP_MID
                        y: 102
                        width: 168
                        height: 56
                        radius: 20
                        bg_color: col_menu_pill
                        bg_opa: COVER
                        border_width: 0
                        scrollbar_mode: "OFF"
                        pad_all: 0
                        clickable: true
                        on_click:
                          - lambda: 'id(menu_touch_slot).execute(0);'
                        widgets:
                          - image:
                              id: slot_center_img
                              align: LEFT_MID
                              x: 14
                              src: menu_img_light
                          - label:
                              id: slot_center_lbl
                              x: 54
                              y: 6
                              text_font: font_menu_item
                              text_color: col_text_pri
                              width: 106
                              long_mode: DOT
                              text: "Home"
                          - label:
                              id: slot_center_sub
                              x: 54
                              y: 30
                              text_font: font_menu_sub
                              text_color: col_menu_val
                              width: 106
                              long_mode: DOT
                              text: "Clock"

                    - obj:
                        id: slot_near_dn
                        align: TOP_MID
                        y: 162
                        width: 180
                        height: 40
                        bg_opa: TRANSP
                        border_width: 0
                        scrollbar_mode: "OFF"
                        pad_all: 0
                        clickable: true
                        on_click:
                          - lambda: 'id(menu_touch_slot).execute(1);'
                        widgets:
                          - image:
                              id: slot_near_dn_img
                              align: LEFT_MID
                              x: 2
                              src: menu_img_ac
                          - label:
                              id: slot_near_dn_lbl
                              align: LEFT_MID
                              x: 38
                              text_font: font_menu_item
                              text_color: col_menu_dim
                              text: "AC"
                              text_opa: 50%

                    - obj:
                        id: slot_far_dn
                        align: TOP_MID
                        y: 198
                        width: 180
                        height: 40
                        bg_opa: TRANSP
                        border_width: 0
                        scrollbar_mode: "OFF"
                        pad_all: 0
                        clickable: true
                        on_click:
                          - lambda: 'id(menu_touch_slot).execute(2);'
                        widgets:
                          - image:
                              id: slot_far_dn_img
                              align: LEFT_MID
                              x: 2
                              src: menu_img_music
                          - label:
                              id: slot_far_dn_lbl
                              align: LEFT_MID
                              x: 38
                              text_font: font_menu_item
                              text_color: col_menu_dim
                              text: "Music"
                              text_opa: 25%

              - obj:
                  id: btn_menu_home
                  align: TOP_RIGHT
                  x: -8
                  y: 8
                  width: 34
                  height: 34
                  radius: 17
                  bg_color: col_menu_pill
                  bg_opa: TRANSP
                  border_width: 0
                  clickable: false
                  scrollbar_mode: "OFF"
                  widgets:
                    - image:
                        src: menu_img_home
                        align: CENTER
                        opa: TRANSP

              - obj:
                  id: sdot_0
                  align: RIGHT_MID
                  x: -10
                  y: -28
                  width: 5
                  height: 5
                  radius: 3
                  bg_color: col_menu_sdot_dim
                  bg_opa: COVER
                  border_width: 0
                  clickable: false
                  scrollbar_mode: "OFF"
              - obj:
                  id: sdot_1
                  align: RIGHT_MID
                  x: -10
                  y: -14
                  width: 5
                  height: 5
                  radius: 3
                  bg_color: col_menu_sdot_dim
                  bg_opa: COVER
                  border_width: 0
                  clickable: false
                  scrollbar_mode: "OFF"
              - obj:
                  id: sdot_2
                  align: RIGHT_MID
                  x: -10
                  y: 0
                  width: 5
                  height: 5
                  radius: 3
                  bg_color: col_menu_accent
                  bg_opa: COVER
                  border_width: 0
                  clickable: false
                  scrollbar_mode: "OFF"
              - obj:
                  id: sdot_3
                  align: RIGHT_MID
                  x: -10
                  y: 14
                  width: 5
                  height: 5
                  radius: 3
                  bg_color: col_menu_sdot_dim
                  bg_opa: COVER
                  border_width: 0
                  clickable: false
                  scrollbar_mode: "OFF"
              - obj:
                  id: sdot_4
                  align: RIGHT_MID
                  x: -10
                  y: 28
                  width: 5
                  height: 5
                  radius: 3
                  bg_color: col_menu_sdot_dim
                  bg_opa: COVER
                  border_width: 0
                  clickable: false
                  scrollbar_mode: "OFF"
              - obj:
                  id: menu_arrow
                  align: RIGHT_MID
                  x: -26
                  y: -35
                  width: 4
                  height: 16
                  radius: 2
                  bg_color: col_menu_accent
                  bg_opa: COVER
                  border_width: 0
                  clickable: false
                  scrollbar_mode: "OFF"
